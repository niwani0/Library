# wiki/ — distilled

Evergreen topic pages, one per topic. **Pages here are harvested from
shipped projects via `/harvest` — never written directly.** Every page
cites the `output/` entries it was distilled from in a `## Harvested from`
section. A page with no source is a bug: demote it to `inbox/`.

Previous stage: [[../output/_index|output/]] — this is the end of the pipeline.

## Topics

*(empty — the first page appears after the first harvest)*
